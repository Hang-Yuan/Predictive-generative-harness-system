#!/bin/bash
printf '{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"Memory signal detection (P/S can be written to candidate pool in real-time at High surprise; E-class written after daily-review confirmation)\nP→MEMORY/procedural_memory (procedural layer): corrected AI behavior patterns, reproducible situation→action patterns\nS→MEMORY/semantic_memory (semantic layer): user preferences, cognitive frameworks, value judgments, directional decisions\nE→project main doc + _this_week (episodic layer): conclusions, open questions, stage decisions, framework updates\nRouting: falls under \"AI encounters X does Y\"→P; falls under \"user is/holds/believes\"→S; identity layer conflict (contradicts USER/persona_SOUL)→do not modify identity layer, enter candidate pool as High surprise\n\nSchema comparison (internalized execution per turn, no output):\n1. Scan candidate pool (P/S) entries, find schema matching current input context\n2. Match+expected=Low (no action); Match+deviant=High (upgrade star or add new, ★★★ entry); No match=Medium (new ★)\n3. High/Medium write: Edit corresponding candidate pool file, S-level silent execution"}}\n'
