#!/bin/bash
printf '{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"每条用户输入后，基于 Schema Prediction 判本次观察两轴：\n- P 轴（预测偏差）：命中 / 击穿 / 未覆盖\n- C 轴（情绪校准信号）：正向确认 / 反对 / 纠正 / 中性\n除 P=命中 且 C=中性 外均值得记。若达到写入下限，追加一行到 <ASSISTANT_ROOT>/MEMORY/episodic_inbox.md 的 ## 活动收件箱 表格末尾：\n| JYYYYMMDD-序号 | YYYY-MM-DD HH:mm | 对话 / close-node / daily-review / weekly-review / 手动补录 | P轴 | C轴 | 程序 / 语义 / 混合 / 未定 | schema 指针 | 一句话观察 | 活动 |\n写入纪律：追加前先读目标 inbox，选择同一文件内当日下一个未占用编号；新行必须严格 9 列、首尾有 |、不得含额外 |，不得插入空行、重建表头或创建第二个活动收件箱。L0 inbox 行级写入 / 删除为 N 级动作：执行后汇总，不问 C 级 verdict。\n细则（轴值展开、字段格式、写入过滤下限）按需调用 MEMORY/00.记忆区_agent.md §两轴判定。"}}\n'
