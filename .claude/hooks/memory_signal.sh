#!/bin/bash
printf '{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"每条用户输入后，基于 Schema Prediction 判本次观察两轴：\n- P 轴（预测偏差）：命中 / 击穿 / 未覆盖\n- C 轴（情绪校准信号）：正向确认 / 反对 / 纠正 / 中性\n除 P=命中 且 C=中性 外均值得记 → 用 Bash 追加一行到 <ASSISTANT_ROOT>/MEMORY/episodic_inbox.md 末尾：\ncat >> \\"<ASSISTANT_ROOT>/MEMORY/episodic_inbox.md\\" <<EOF ... EOF\n细则（轴值展开、字段格式、写入过滤下限）按需调用 MEMORY/00.记忆区_agent.md §两轴判定。"}}\n'
